# YouTube API Configuration
# IMPORTANT: You need to get a valid API key from Google Cloud Console
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable YouTube Data API v3: APIs & Services > Library > YouTube Data API v3 > Enable
# 4. Create credentials: APIs & Services > Credentials > Create Credentials > API Key
# 5. Copy your new API key and replace the value below
YOUTUBE_API_KEY=your_valid_youtube_api_key_here

# Server Configuration (optional)
PORT=3000 